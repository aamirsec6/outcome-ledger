# Admin panel API connection (server-side — preferred)
ADMIN_API_URL=https://your-api.up.railway.app
ADMIN_TOKEN=your-secret-admin-token

# Legacy fallbacks (avoid — token would be exposed in browser bundle)
# NEXT_PUBLIC_ADMIN_API_URL=
# NEXT_PUBLIC_ADMIN_TOKEN=
